<% @taxons = @taxon && @taxon.parent ? @taxon.parent.children : Spree::Taxon.roots %>
<%= form_tag spree.products_path, method: :get do %>
	<div class="row">
	  <% cache [I18n.locale, @taxons] do %>
	    <%#= select_tag :taxon,
	          options_for_select([[t('spree.all_departments'), '']] +
	                                @taxons.map {|t| [t.name, t.id]},
	                                @taxon ? @taxon.id : params[:taxon]), 'aria-label' => 'Taxon' %>
	  <% end %>
	</div>
	<div class="">
	  <%= search_field_tag :keywords, params[:keywords], placeholder: 'Search in Store', class: 'form form-control' %>
	</div>
	<br>
	<div class="">
	  <%= submit_tag t('spree.search'), name: nil, class: 'btn btn-info btn-block' %>
	</div>
<% end %>
